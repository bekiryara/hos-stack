name: Product E2E Gate

on:
  pull_request:
    branches: [main, develop]
    paths:
      - 'work/pazar/routes/api.php'
      - 'work/pazar/app/Http/Controllers/Api/**/*.php'
      - 'ops/product_e2e.ps1'
      - '.github/workflows/product-e2e.yml'
  push:
    branches: [main, develop]
    paths:
      - 'work/pazar/routes/api.php'
      - 'work/pazar/app/Http/Controllers/Api/**/*.php'
      - 'ops/product_e2e.ps1'
      - '.github/workflows/product-e2e.yml'

jobs:
  product-e2e:
    runs-on: ubuntu-latest
    env:
      BASE_URL: "http://localhost:8080"
      HOS_BASE_URL: "http://localhost:3000"
      PRODUCT_TEST_TENANT_ID: ${{ secrets.PRODUCT_TEST_TENANT_ID }}
      PRODUCT_TEST_AUTH_TOKEN: ${{ secrets.PRODUCT_TEST_AUTH_TOKEN }}
      PRODUCT_TEST_TENANT_B_ID: ${{ secrets.PRODUCT_TEST_TENANT_B_ID }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup PowerShell
        uses: microsoft/setup-powershell@v1
        with:
          pwsh-version: '7.4'
      
      - name: Bring up core stack
        run: |
          docker compose up -d --build
          # Wait for services to be ready
          timeout 120 bash -c 'until curl -f http://localhost:8080/up && curl -f http://localhost:3000/v1/health; do sleep 5; done' || true
      
      - name: Run Product E2E Gate
        shell: pwsh
        run: |
          pwsh -NoProfile -ExecutionPolicy Bypass -File ./ops/product_e2e.ps1
        continue-on-error: false
      
      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: product-e2e-logs
          path: |
            _archive/audits/*
            _archive/incidents/*
          retention-days: 7
      
      - name: Generate incident bundle on failure
        if: failure()
        shell: pwsh
        run: |
          if (Test-Path ./ops/incident_bundle.ps1) {
            pwsh -NoProfile -ExecutionPolicy Bypass -File ./ops/incident_bundle.ps1
          }
        continue-on-error: true
      
      - name: Upload incident bundle on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: product-e2e-incident-bundle
          path: |
            _archive/incidents/*
          retention-days: 7
      
      - name: Cleanup
        if: always()
        run: |
          docker compose down
