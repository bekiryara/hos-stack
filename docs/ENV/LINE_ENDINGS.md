# Line endings (EOL)

Repo line endings are locked via `.gitattributes`. Use **LF** in editors for web/source files; **CRLF** for `*.ps1` (Windows scripts). Normalize only when instructed (e.g. targeted `git add --renormalize` for specific files).
