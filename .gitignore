# Secrets / private
work/hos/secrets/*.txt
**/secrets/*
.env
.env.*
**/.env
oidc_token*.json
oidc_userinfo.json
pazar_oidc_discovery.json
cookies.txt
headers.txt

# Graveyard (quarantined code - preserve but don't track)
_graveyard/

# Archive / diagnostics / backups (never track)
_archive/
# Release bundles (never track)
_archive/releases/
# Self-audit records (never track)
_archive/audits/
# Daily snapshots (never track)
_archive/daily/
_diag_*.txt
_diagnosis_*.txt
_evidence_*.txt
_evidence_*.json
_verify_output*.txt
*.bak-*
*.backup-*
.env.backup-*

# Ops temporary files
ops/*.backup
ops/diffs/

# Draft / local-only category seed helpers (not wired yet)
work/pazar/database/seed_data/

# Local imports / downloaded datasets (never track)
work/_imports/

# Test results and temporary JSON files
*_tests_results.json
ops_gates_output.json

# Gate run logs (never track; Public Ready requires clean repo)
docs/PROOFS/_logs/

# Temp files
_temp_*.txt

# Pazar scripts: local scratch + captured outputs (never track)
work/pazar/scripts/_tmp_*.ndjson
work/pazar/scripts/hepsiemlak-captured-urls*.json
work/pazar/scripts/hepsiemlak-realty-list*.ndjson
work/pazar/scripts/hepsiemlak-extract-ndjson.mjs
work/pazar/scripts/hepsiemlak-export-*.mjs
work/pazar/scripts/hepsiemlak-extract-filter-schema.mjs
work/pazar/scripts/hepsiemlak-cdp-sniffer.mjs
work/pazar/scripts/hepsiemlak-generate-real-estate-rules.mjs
work/pazar/scripts/verify-hepsiemlak-filter-schema.mjs
work/pazar/scripts/start-hepsiemlak-cdp.ps1
work/pazar/scripts/verify-karavan-schema.js
work/pazar/scripts/arabam-export-karavan-schema.js

# Node modules (should never be tracked)
**/node_modules/
work/marketplace-web/node_modules/

# Vendor (should never be tracked)
**/vendor/
work/pazar/vendor/
**/dist/
work/marketplace-web/dist/
**/.vite/

# Editor / workspace settings (local)
.vscode/

# Local Codex proxy / debug artifacts (local only)
.codex-proxy-home/
.codex-proxy-test/
codex-proxy.cmd
codex-proxy.ps1
debug.log
