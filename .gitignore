# Secrets / private
work/hos/secrets/*.txt
.env
.env.*
oidc_token*.json
cookies.txt
headers.txt

# Graveyard (quarantined code - preserve but don't track)
_graveyard/

# Archive / diagnostics / backups (never track)
_archive/
# Release bundles (never track)
_archive/releases/
# Self-audit records (never track)
_archive/audits/
# Daily snapshots (never track)
_archive/daily/
_diag_*.txt
_diagnosis_*.txt
_evidence_*.txt
_evidence_*.json
_verify_output*.txt
*.bak-*
*.backup-*
.env.backup-*

# Ops temporary files
ops/*.backup
ops/diffs/

# Temp files
_temp_*.txt

# Vendor / dependencies (NO TRACK policy)
work/pazar/vendor/
work/pazar/node_modules/
**/node_modules/
**/dist/
_tmp*
.DS_Store
